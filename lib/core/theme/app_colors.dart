import 'package:flutter/material.dart';

/// App Color Palette

class AppColors {
  AppColors._();

  // ─────────────────────────────────────────
  // PRIMARY
  // ─────────────────────────────────────────

  /// --Main-Blue-Indigo-Dye (#035DAD)
  static const Color mainBlueIndigoDye = Color(0xFF035DAD);

  /// --Main-Blue-Secondary (#173A62)
  static const Color mainBlueSecondary = Color(0xFF173A62);

  /// --Main-Dark-Blue (#1D2832)
  static const Color mainDarkBlue = Color(0xFF1D2832);

  /// --Main-Orange (#EE8144)
  static const Color mainOrange = Color(0xFFEE8144);

  /// --Main-Neutral (#DAD9D9)
  static const Color mainNeutral = Color(0xFFDAD9D9);

  // ─────────────────────────────────────────
  // SECONDARY - Highlight
  // ─────────────────────────────────────────

  /// --Highlight-Darkest (#006FFD)
  static const Color highlightDarkest = Color(0xFF006FFD);

  /// --Highlight-Dark (#2897FF)
  static const Color highlightDark = Color(0xFF2897FF);

  /// --Highlight-Medium (#6FBAFF)
  static const Color highlightMedium = Color(0xFF6FBAFF);

  /// --Highlight-Light (#B4DBFF)
  static const Color highlightLight = Color(0xFFB4DBFF);

  /// --Highlight-Lightest (#EAF2FF)
  static const Color highlightLightest = Color(0xFFEAF2FF);

  // ─────────────────────────────────────────
  // NEUTRAL - Light
  // ─────────────────────────────────────────

  /// --Neutral-Light-Darkest (#C5C6CC)
  static const Color neutralLightDarkest = Color(0xFFC5C6CC);

  /// --Neutral-Light-Dark (#D4D6DD)
  static const Color neutralLightDark = Color(0xFFD4D6DD);

  /// --Neutral-Light-Medium (#E8E9F1)
  static const Color neutralLightMedium = Color(0xFFE8E9F1);

  /// --Neutral-Light-Light (#F8F9FE)
  static const Color neutralLightLight = Color(0xFFF8F9FE);

  /// --Neutral-Light-Lightest (#FFFFFF)
  static const Color neutralLightLightest = Color(0xFFFFFFFF);

  // ─────────────────────────────────────────
  // NEUTRAL - Dark
  // ─────────────────────────────────────────

  /// --Neutral-Dark-Darkest (#1F2024)
  static const Color neutralDarkDarkest = Color(0xFF1F2024);

  /// --Neutral-Dark-Dark (#2F3036)
  static const Color neutralDarkDark = Color(0xFF2F3036);

  /// --Neutral-Dark-Medium (#494A50)
  static const Color neutralDarkMedium = Color(0xFF494A50);

  /// --Neutral-Dark-Light (#71727A)
  static const Color neutralDarkLight = Color(0xFF71727A);

  /// --Neutral-Dark-Lightest (#8F9098)
  static const Color neutralDarkLightest = Color(0xFF8F9098);

  // ─────────────────────────────────────────
  // SUPPORT - Success
  // ─────────────────────────────────────────

  /// --Support-Success-Dark (#298267)
  static const Color successDark = Color(0xFF298267);

  /// --Support-Success-Medium (#3AC0A0)
  static const Color successMedium = Color(0xFF3AC0A0);

  /// --Support-Success-Light (#E7F4E8)
  static const Color successLight = Color(0xFFE7F4E8);

  // ─────────────────────────────────────────
  // SUPPORT - Warning
  // ─────────────────────────────────────────

  /// --Support-Warning-Dark (#E86339)
  static const Color warningDark = Color(0xFFE86339);

  /// --Support-Warning-Medium (#FFB37C)
  static const Color warningMedium = Color(0xFFFFB37C);

  /// --Support-Warning-Light (#FFF4E4)
  static const Color warningLight = Color(0xFFFFF4E4);

  // ─────────────────────────────────────────
  // SUPPORT - Error
  // ─────────────────────────────────────────

  /// --Support-Error-Dark (#ED3241)
  static const Color errorDark = Color(0xFFED3241);

  /// --Support-Error-Medium (#FF616D)
  static const Color errorMedium = Color(0xFFFF616D);

  /// --Support-Error-Light (#FFE2E5)
  static const Color errorLight = Color(0xFFFFE2E5);

  // ─────────────────────────────────────────
  // COMMON
  // ─────────────────────────────────────────

  /// Pure White (#FFFFFF)
  static const Color white = Color(0xFFFFFFFF);

  /// Pure Black (#000000)
  static const Color black = Color(0xFF000000);

  /// Transparent
  static const Color transparent = Colors.transparent;
}
